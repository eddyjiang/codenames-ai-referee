node_modules/
dist/
.wrangler/
.env
.env.local
*.local

# Claude Code local settings (machine-specific; never commit)
.claude/settings.local.json

# Wrangler local secrets (contains API keys — never commit).
# Bare name also matches a .dev.vars in any other package, not just worker/.
.dev.vars
worker/.dev.vars

# Python (cv-service)
__pycache__/
*.py[cod]
.venv/
*.egg-info/
cv-service/.env

# Build outputs
frontend/dist/
worker/dist/

# D1 local database
.wrangler/state/

# Test images
test-boards/

# OS
.DS_Store
Thumbs.db
